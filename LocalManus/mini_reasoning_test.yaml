model:
  model_name: gpt-5.4
  model_class: litellm_textbased
  cost_tracking: ignore_errors
  model_kwargs:
    api_base: https://api.gameron.me/v1
    api_key: sk-user-e65550a3cd582639241fdb153dfaca4e
    max_tokens: 512
    reasoning_effort: high
agent:
  step_limit: 4
  mode: yolo
  output_path: /tmp/mini_reasoning_test.traj.json
  system_template: |
    You are a Linux agent. Run exactly one bash command per response.
    When finished, submit:
    ```mswea_bash_command
    echo COMPLETE_TASK_AND_SUBMIT_FINAL_OUTPUT
    ```
  instance_template: |
    TASK: {{task}}
    System: {{system}} {{release}} {{machine}}
environment:
  cwd: /home/justinleopard/projects
  environment_class: local
  env:
    PAGER: cat